rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 원본 .ent 파일은 업로드한 학생 uid 본인만 다시 읽고 쓸 수 있다.
    match /entries/{uid}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
